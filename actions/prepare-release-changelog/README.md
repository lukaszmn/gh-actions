# prepare-release-changelog

Extracts release notes from the `Unreleased` section and rolls the changelog forward to the current version.

Main inputs:
- `version_tag`: release tag such as `v1.2.3`
- `changelog_path`: changelog file path
- `release_body_path`: file path for extracted release notes
