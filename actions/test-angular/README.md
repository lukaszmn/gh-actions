# test-angular

Checks out the repository, installs Node.js dependencies, and runs the configured frontend test command.

Main inputs:
- `node_version`: Node.js version to install
- `working_directory`: frontend working directory
- `test_command`: command used to run tests
