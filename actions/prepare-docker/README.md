# prepare-docker

Checks out the repository, logs the selected release artifacts, authenticates to a container registry, and sets up Docker Buildx.

Main inputs:
- `registry_username`: registry login username
- `registry_password`: password
